import postcss from 'postcss';
import plugin from 'postcss-colorguard';
export { default } from 'postcss-colorguard';

type Options = {
  whitelist?: string[][];
  allowEquivalentNotation?: boolean;
  ignore?: string[];
  threshold?: number;
};

declare const processor: (opts: Options) => postcss.Processor;

export { processor };
